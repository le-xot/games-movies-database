{
  "name": "lexot-website",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:frontend": "bun --filter=./frontend run dev",
    "dev:backend": "bun --filter=./backend run dev",
    "build": "bun run --filter '*' build",
    "build:frontend": "bun --filter=./frontend run build",
    "build:backend": "bun --filter=./backend run build",
    "start:backend": "bun --filter=./backend run start",
    "prisma": "bun --filter=./backend run prisma migrate dev && bun --filter=./backend run prisma generate",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "typecheck": "bun --filter=./frontend run typecheck"
  },
  "dependencies": {
    "typescript": "6.0.2"
  },
  "devDependencies": {
    "@types/node": "26.4.0",
    "oxfmt": "0.66.0",
    "oxlint": "1.81.0"
  },
  "trustedDependencies": [
    "@nestjs/core",
    "@parcel/watcher",
    "@prisma/engines",
    "prisma",
    "sass-embedded",
    "vite",
    "vue-demi",
    "vue-tsc"
  ]
}
